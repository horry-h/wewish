# 每日一签 - 快速参考

## 三大核心增强功能

### 🔊 1. 摇签音效
```typescript
// 开始摇动 → 播放音效
playShakeSound()

// 停止摇动 → 暂停音效  
stopShakeSound()

// 自动控制：超过1秒无摇动自动暂停
```

**状态**: ✅ 已实现（临时使用翻页音效）  
**建议**: 替换为真实摇签音效 `shake-fortune.wav`

---

### ⚡ 2. AI预调用（秒开）
```typescript
onShakeStart() {
  this.playShakeSound()
  this.generateFortune() // 立即调用，不等摇完
}
```

**原理**: 用户摇动2-3秒 = AI接口耗时  
**结果**: 签条掉落时AI已就绪 → 秒开体验

---

### 📅 3. 每日限额
```typescript
// 保存今日签文
wx.setStorageSync('last_fortune_date', '2026-01-30')
wx.setStorageSync('today_fortune', fortuneData)

// 检查是否已抽签
checkTodayFortune()
```

**逻辑**:
- 今天首次 → 允许摇签
- 今天再次进入 → 自动显示历史签文
- 尝试重复抽签 → 提示"明天再来"

---

## 关键参数配置

```typescript
shakeThreshold: 1.2        // 加速度阈值
shakeRequiredCount: 8      // 需要摇动8次
shakeAudio.volume: 0.6     // 音量60%
shakeAudio.loop: true      // 循环播放
```

---

## 用户体验时间线

```
T=0s    开始摇动 → 播放音效 + 调用AI
T=2s    摇够8次 → 停止音效 + 签条掉落
T=3s    签条完全掉落
T=3.5s  显示结果（AI已就绪）✨秒开
```

---

## 测试检查清单

- [ ] 摇动时能听到音效
- [ ] 停止摇动音效自动暂停
- [ ] 摇够次数签条掉落
- [ ] 结果秒开无等待
- [ ] 今天二次进入显示历史签文
- [ ] 关闭后提示"明天再来"
- [ ] 第二天可以重新抽签

---

## 文件位置

```
pages/fortune/
├── fortune.wxml          # 页面结构
├── fortune.ts            # 业务逻辑 ⭐
├── fortune.wxss          # 样式
├── fortune.json          # 配置
├── README.md             # 完整文档
└── AUDIO_README.md       # 音效说明
```

---

## 快速修改音效

1. 准备音效文件 `shake-fortune.wav`
2. 放入 `/assets/audio/` 目录
3. 修改 `fortune.ts` 第102行:

```typescript
this.shakeAudio.src = '/assets/audio/shake-fortune.wav'
```

---

**就是这么简单！** 🎉
